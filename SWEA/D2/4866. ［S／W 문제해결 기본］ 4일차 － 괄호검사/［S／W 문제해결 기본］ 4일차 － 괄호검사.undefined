T = int(input())

for tc in range(1, T+1):
    sentence = input()

    flag = 1
    top = -1
    stack = [0] * len(sentence)
    pair = {')': '(',
            ']': '[',
            '}': '{',
            '>': '<'}

    for s in sentence:
        if s in '([{<':
            top += 1
            stack[top] = s
        elif s in '>}])':
            if top == -1:
                flag = 0
                break
            else:
                top -= 1
                tmp = stack[top + 1]
                if pair[s] != tmp:
                    flag = 0
                    break

    if top != -1:
        flag = 0

    print(f"#{tc} {flag}")